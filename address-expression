Go to https://regexr.com
Expression for the input: [0-9].+[A-Za-z0-9]\d

Should find complete address, still connect any sequence of characters in between a patch of number & then end at a line break.
Address is one of the hardest items to discern from the rest of the code but this is a start, just have to eliminate non addresses manually
